name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    # Закреплённый образ, а не ubuntu-latest: смена образа GitHub
    # (ubuntu-latest переходит на 26 с 19.10.2026) не должна незаметно
    # менять условия эксперимента.
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v7

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Verify all critical files are present
        run: |
          python3 scripts/verify_release.py

      - name: Run tests
        run: |
          PYTHONPATH=. pytest tests/ -v

      - name: Run benchmark suite
        run: |
          PYTHONPATH=. python3 -m benchmark.run_benchmark

      - name: Framework Evaluation smoke test
        run: |
          cd examples/framework_evaluation
          PYTHONPATH=../../ python3 test_pipeline.py
