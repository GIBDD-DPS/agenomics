# Ядро методологии (agenomics/) не имеет внешних зависимостей,
# кроме стандартной библиотеки Python 3.9+.

# Для тестов:
pytest>=7.0

# Для будущего веб-API (v0.2, см. amvera.yml) — раскомментировать при реализации:
# fastapi>=0.110
# uvicorn>=0.29
