"""
benchmark — Agenomics Synthetic Benchmark Suite v0.1.

Автор: Dm.Andreyanov
Проект: Prizolov Lab

ЧЕСТНАЯ ОГОВОРКА (читать перед использованием результатов):

Этот набор бенчмарков проверяет ВНУТРЕННЮЮ СОГЛАСОВАННОСТЬ формул
методологии (детерминированность, монотонность, скорость реакции на
синтетически внедрённые сценарии) — НЕ предсказательную силу Trust
Score относительно поведения РЕАЛЬНЫХ агентов в проде.

Это принципиально разные вещи:
  - "Formula is internally consistent" (что тестирует этот набор) —
    доказуемо синтетически, прямо сейчас, без внешних данных.
  - "Formula predicts real-world incidents" (что НЕ тестирует этот
    набор) — требует накопленных реальных данных через GenomeLedger/
    IncidentFeedback за продолжительный период эксплуатации; таких
    данных пока не существует ни у кого, кто использует методологию.

Метрика Incident Correlation ниже явно помечена как not_computable —
не потому что лень, а потому что вычислить её синтетически без
подмены реальных данных нельзя, не будучи в этом честным.
"""

from .metrics import run_all_benchmarks

__all__ = ["run_all_benchmarks"]
